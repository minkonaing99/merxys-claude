# Taste Profile

Concrete design moves extracted from winners the user actually picked. The **derived** narrative — read in Stage 0 for the Design Read. Raw pick data lives in `taste-signals.jsonl` (append-only, one row/run); this file is rewritten from it by the Stage 5 distill (~every 10 rows), NOT appended per-run. Future Design Reads start from here.

Each entry: date · page kind · what was picked (font / palette / layout / motion / density) · one-line why it won. Rows below predate the signals log (historical, hand-written); new runs flow through `taste-signals.jsonl` → distill.

| Date | Page kind | Type | Palette | Layout | Motion | Density | Why it won |
|---|---|---|---|---|---|---|---|
| 2026-06-26 | Landing hero (DTC coffee) | Fraunces display, light weight (300) at huge size (clamp→180px), italic accent word in contrast color | Warm dawn: cream `#fff6ea` bg, ink `#241405`, dawn-orange accents `#f4a259`/`#d96b3a`, radial sun glow | Left-aligned oversized two-line headline w/ italic second line, blurred radial sun top-right, horizontal stat strip below | Pill buttons w/ translateY lift + soft shadow on hover (M5) | Airy (D4 but generous whitespace) | Picked the EXPLORE lane over two safer variants. Chose warmth + drama over restraint despite asking for "calm". Wants brand-forward boldness, big expressive serif, atmospheric color, NOT swiss/minimal. |
| 2026-06-27 | Full landing (Pizza House) | Fraunces 400/300 italic accent word in gold; oversized display (clamp→8.5rem); Hanken Grotesk body | Dark wood-fired: char-black `#140d08` bg, cream `#f7ece0` text, ember `#f2691e`/`#ff8c42`, gold `#f4b860` accent, ember radial glow | Full-bleed photo hero w/ dark warm overlay; alternating big-photo menu ROWS (refused tidy card grid); centered big-quote story; ember radial CTA | Pill btn translateY lift + glow shadow (M6); scroll parallax + side-slide reveals on winner | Bold (D4), full-bleed imagery as loudest element | Picked lane C (Wood-Fired Maximal V8/M6/D4) over light-editorial (A) and the literal restrained brief (B). Brief said "warm, family-friendly, not corporate" → reads quiet; picked the DARKEST, BOLDEST, most photo-forward lane. Same pattern as coffee. |
| 2026-06-28 | Full site (Natural Mountain dairy) | **A:** Fraunces 300/400 + terracotta italic accent + Hanken body. **C:** Bricolage Grotesque 700/800 + gold italic accent + Hanken body | **A:** warm cream `#F6F0E2`, ink `#241A12`, sage, butter `#E8B04B`, terracotta `#BC5E36`. **C:** deep pine `#13241A`, cream `#F4EBD9`, gold `#F0B43C`, berry, moss | **A:** side-by-side serif hero + soft photo cards + editorial provenance rows. **C:** full-bleed pasture-cow hero + immersive overlay cards (asymmetric span) + alternating photo rows | M6 both: scroll reveals (y+fade), hero parallax (scrub), card hover lift, stat count-up; gsap.matchMedia + reduced-motion | A airy D3 · C bold D4 | **Picked BOTH the warm-LIGHT editorial (A) AND the dark maximal (C)** — wanted two sites, not one. First time a LIGHT lane survived alongside the dark one. Rejected the Swiss-restrained lane (B) outright. Through-line held: expressive display serif/organic + italic accent word + real photography as the loudest element. Temperature (light vs dark) is negotiable; the warmth + photo-forward + big-display DNA is not. |

| 2026-07-03 | Full landing (Komment — dev-tool, API-docs SaaS) | Inter 300 thin display (6vw clamp→68px), gradient accent phrase (indigo→magenta text clip); JetBrains Mono for code/signatures; tabular-nums in pricing | Stripe-light: white `#fff` canvas, soft `#f6f9fc` bands, indigo `#533afd` single accent, magenta `#f96bee`+cyan mesh (hero upper-third + CTA glow ONLY), ink `#0d253d` | Centered thin-weight hero + gradient-mesh top third; elevated code→docs split panel (dark navy code `#0d1424` / white doc); soft white cards w/ hover-lift; 3-col pricing w/ elevated Team card | M3 subtle: IntersectionObserver fade+18px reveals, cubic-bezier(.16,1,.3,1); btn translateY + arrow nudge; reduced-motion gated; NO animation lib | Airy-technical (dense code, generous whitespace) | Picked the LIGHT/restrained Stripe lane over the dark Linear lane. FIRST dev-tool job + FIRST time a restrained lane won — because backend-engineer audience + named refs (Linear/Stripe) make precision the brief, not drama. Domain overrides the food-era "always bold" bias. |
| 2026-07-04 | Full landing (ForgeLoop — user's OWN software studio, B2B services) | Archivo 700/800 uppercase display (clamp→80px, lh~1.0), ember accent word in H1; JetBrains Mono for labels/indices/nav/buttons | Forge industrial: charcoal `#141414`/`#17181a`, warm off-white text, single molten ember `#ff5a1f`, ZERO blue/indigo (banned in contract) | Blueprint frame: hairline side rules + measurement ticks + ember corner brackets; terminal build-log hero artifact (FL-2026-047, phase bars); mono-indexed spec cards (W-001..003) for work; stamped "Inspected-Pass" testimonial records; squared buttons | M4: IO staggered reveals (17px/550ms), scaleX bar growth, stat count-ups, -2px hover lifts; `.js-motion` class gate + reduced-motion | Dense-technical D4 | Picked the EXPLORE lane on its lane DESCRIPTION alone — before gates, screenshots, or judging. Forge-metaphor-literal beat both brand-ref lanes (Linear dark, Stripe light). Then asked for MORE content: stats band, selected-work case cards, 3 testimonials, FAQ — "as a good software company" = fuller, credibility-heavy page. |
| 2026-07-10 | **Product register** (xpenses — solo expense-tracker PWA, app UI not landing) | System font stack (SF Pro proxy), no display serif/gradient anywhere; tabular-nums on all money; Phosphor icons | Apple Wallet-restrained: canvas `#f5f5f7`, surface `#fff`, single primary `#0066cc`, semantic success/warning/error only, zero decorative color | "Quiet Card System": ONE flat elevated surface (18px radius, single soft shadow) per screen holding grouped hairline rows — not a repeated card grid, not a bare ledger either | M3, CSS-only: 150-200ms ease-out, `:active{scale(0.95)}`, skeleton shimmer. MOTION dial stayed under the GSAP threshold (>4) — no animation lib needed | Airy D3 | FIRST product-register (app/dashboard) job in this profile — `taste-skill` Generator A is out of scope here by design, only `impeccable` fires. User picked the CALMER of two restrained lanes (VARIANCE 5 single-panel over VARIANCE 3 bare hairline ledger) — i.e. even within "restrained," a single soft elevated surface reads better than zero elevation at all. Directly inverts the landing-era "restrained/Swiss/cold loses" signal below — that signal is scoped to food/brand landing pages; for solo-use utility app UI, restrained is the brief and wins outright. |

## Signals (running)
- **REGISTER GATES THE TASTE EVEN HARDER THAN DOMAIN.** All signals below (bold/dark/photo-forward wins, restrained loses) come from **brand-register landing pages**. The first **product-register** job (xpenses, 2026-07-10) inverted the "restrained loses" signal completely: for app/dashboard UI, calm + single accent + zero decoration IS the brief and won outright, no explore-lane bold alternative was ever in contention (Generator A `taste-skill` doesn't even fire for product register). Don't apply the food/landing bold-bias below to app UI, admin panels, or dashboards — check register first, domain second.
- **DOMAIN GATES THE TASTE — the food-era biases below do NOT generalize.** First dev-tool job: user picked the LIGHT, restrained, Stripe-precise lane over the dark maximal Linear lane. For technical/developer/B2B audiences favour precision, whitespace-as-luxury, a single accent color, thin editorial display, and a real product-UI mockup (code→docs panel) as the hero element — NOT warmth/photography/drama. Scope every food signal below to food/drink/lifestyle briefs.
- **When the brief names real brands, ground in their measured `DESIGN.md`** (awesome-design-md, 74 brands). Two opposite-temperature refs (Linear dark vs Stripe light) hand you two divergent lanes for free — cheap and accurate, beats inventing.
- **Dev-tool hero = a built UI mockup, not stock imagery.** A source→generated-docs split panel in HTML/CSS out-reads any photo for this audience. Syntax-highlight purple is domain-correct, NOT an AI-slop tell.
- **Motion dial ≤3 = IntersectionObserver, not GSAP.** For "subtle scroll reveals," native IO at zero bundle cost is right; reserve GSAP for choreographed pin/scrub/timeline.
- **The real constant (3/3 food jobs): expressive big display + italic accent word in a contrast color + REAL photography as the loudest element.** Temperature is negotiable — dark won twice (coffee glow / pizza char), and on the dairy job the user kept BOTH a warm-LIGHT editorial AND a dark maximal. Don't assume dark; assume warm + photo-forward + a dramatic display face with one italic accent word. Refine of the old "always darkest" read.
- **Says "calm/clean/family" but never picks the restrained lane. CONFIRMED 3/3** (coffee→EXPLORE, pizza→dark maximal, dairy→rejected the Swiss-clean lane though brief said "mountain-clean"). Discount stated restraint ~1 notch; ALWAYS keep ≥1 bold lane. State the warning up front so they can override with words.
- **Photography is non-negotiable for food/drink.** Verify image URLs render before judging (a broken/wrong photo sinks a good layout). Real pasture/product shots beat icons/illustration.
- Loves **expressive display**: Fraunces 300 (light serif) and Bricolage Grotesque 700/800 (organic) both won. Italic accent word every time.
- Comfortable with **gradients + glow + radial atmosphere**; never picks the gradient-banned restrained variant.
- Pill buttons + soft shadow read premium. Squared/sharp buttons (the Swiss lane) lost.
- **Will take TWO winners when torn** — don't force a single pick if two lanes both land; offer to ship both.

## Rejections (#4 — losers carry signal)
What was passed over and why. Anti-preferences converge taste faster. Append a row per losing variant.

| Date | Page kind | Rejected lane | Why it lost |
|---|---|---|---|
| 2026-06-26 | Coffee hero | Swiss-grid / restrained (gradient-banned) | too cold / minimal — user wants warmth + drama |
| 2026-06-26 | Coffee hero | Editorial safe | not bold enough vs the explore lane |
| 2026-06-27 | Pizza landing | Light-editorial (A) | reads quiet; food needs appetite/photography |
| 2026-06-27 | Pizza landing | Restrained literal-brief (B) | "family-friendly" taken literally = generic, passed over |
| 2026-06-28 | Natural Mountain (dairy) | Creamery Swiss (B) — restrained hairline grid, uppercase Hanken, squared buttons, pine accent | Too cold/restrained; squared buttons + Swiss grid read as catalogue, not appetite. 3rd straight loss for the restrained/Swiss lane even when the brief literally said "mountain-clean". Note: A (light-editorial) did NOT lose this time — the user took it as one of two winners — so "light editorial loses" is NOT a rule; "restrained/Swiss/cold loses" IS. |
| 2026-07-03 | Komment (dev-tool) | Linear dark lane (A) — near-black `#010102`, single lavender accent, hairline-structural, dense left-aligned hero | Lost to the light Stripe lane for a backend-engineer landing. NOT a "dark loses" signal — dark was a faithful, strong execution; the user simply chose the lighter, airier, more marketing-forward direction for top-of-funnel. Confirms: for dev-tools BOTH lanes are legitimate; temperature is the real toss-up, so always offer both and let them pick. (Contrast w/ food, where restrained/cold reliably loses.) |
| 2026-07-04 | ForgeLoop (own studio) | A — Linear dark precision (near-black, electric blue, hairline) | Never reached judging: user pre-picked C off lane descriptions. Brand-name-ref lane read generic next to the metaphor-literal lane. |
| 2026-07-04 | ForgeLoop (own studio) | B — Stripe light clean (white, indigo, whitespace-luxury) | Same — pre-picked past it. Second data point that restrained lanes lose when the brief is the USER'S OWN brand (identity wants distinctiveness; client-audience fit picked restrained on Komment). |
| 2026-07-10 | xpenses (product register) | "Restrained Instrument" (Variant A) — hairline divide-y rows, no card wrapper at all, DENSITY 5 | Not "too bold" (opposite problem from every landing job) — lost for being too bare/dense next to the calmer single-panel alternative. Zero elevation read as slightly busier at DENSITY 5; one soft elevated surface (Variant B) communicated "calm instrument" better than a flush ledger. First loss reason in this profile that isn't "too restrained" — useful counter-signal for product register specifically. |
