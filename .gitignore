# Session/runtime state
history.jsonl
.ponytail-active
.last-cleanup
.agents
statusline-metrics.json
.caveman-active
.caveman-active.prev
.caveman-mode-log.jsonl
.playwright/

# Python bytecode
__pycache__/
*.pyc

# Cache
cache/
image-cache/
paste-cache/
mcp-needs-auth-cache.json

# Backups
backups/

# Runtime directories
sessions/
session-env/
shell-snapshots/
tasks/
telemetry/
ide/
file-history/

# Daemon runtime state (contains control.key - never commit)
daemon/
daemon.log
jobs/

# Project-specific data (machine-local paths)
projects/

# Plugin runtime (auto-regenerated on install)
plugins/cache/
plugins/data/
plugins/installed_plugins.json
plugins/.last_inuse_sweep

# Plugin marketplaces (external clones, regenerated on install)
plugins/marketplaces/

# Runtime pid
.caffeinate.pid

# External clone (re-cloneable)
reference/awesome-design-md/
